# liveProject_deepRecommender

## Install

Create a virtual environment with Python 3.9 and install from git:

```bash
pip install git+https://github.com/chris-santiago/liveProject_deepRecommender.git
```

## Use
